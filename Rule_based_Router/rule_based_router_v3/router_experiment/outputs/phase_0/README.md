# Frozen V3 Phase 0 artifacts

These files are the historical baseline generated before Phase 1 algorithm
changes. They must not be overwritten by Phase 1 or later runs.

The metrics file records the Git commit, dataset SHA256, configuration SHA256
and UTC generation timestamp used to create this snapshot.
